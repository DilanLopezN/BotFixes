import styled from "styled-components";
import Page from "../Page";

 const MainMenu = styled(Page)`
    .content {
        padding: 0 !important;
        display: flex;
        overflow: hidden;
        position: relative;
    }
`;

export {
  MainMenu
}