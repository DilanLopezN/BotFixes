import { Space } from 'antd';
import { styled } from 'styled-components';

export const AvatarListContainer = styled.div`
  width: min-content;
  margin-left: auto;
`;

export const AvatarTootipContainer = styled(Space)`
  width: 320px;
  max-height: 40vh;
  overflow: auto;
`;
