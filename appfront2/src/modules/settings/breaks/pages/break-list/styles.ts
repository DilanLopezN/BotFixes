import { Input } from 'antd';
import { styled } from 'styled-components';

export const TableTitle = styled.span`
  font-size: 16px;
`;

export const SearchInput = styled(Input.Search)``;

export const BreaksCountContainer = styled.div`
  padding: 16px 0;
`;
